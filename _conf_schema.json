{
  "ov_base_url": {
    "description": "OpenViking server URL (must be a peer-contract server, i.e. OpenViking with PR #2236 or later)",
    "type": "string",
    "default": "http://localhost:1933"
  },
  "ov_admin_api_key": {
    "description": "Admin API key (only used in self_scope=venue mode to mint a per-venue OV user)",
    "type": "string"
  },
  "ov_user_api_key": {
    "description": "User API key. Used directly as the Bearer identity in self_scope=global mode",
    "type": "string"
  },
  "ov_account_id": {
    "description": "OV account ID (only needed for the admin user-creation URL in self_scope=venue mode)",
    "type": "string"
  },
  "self_scope": {
    "description": "Memory owner (self) granularity: global (one bot self, peers shared across all venues, default) | venue (per-group self, peers isolated per group)",
    "type": "string",
    "default": "global"
  },
  "global_user_id": {
    "description": "OV user ID for the bot self in global mode when only an admin key is available (minted once). Ignored when ov_user_api_key is set.",
    "type": "string",
    "default": "astrbot-global"
  },
  "isolation_overrides": {
    "description": "Per-group self_scope overrides as JSON, e.g. {\"123456\": \"venue\"}. Legacy values (global_user/venue_user/venue_user_fanout) are auto-mapped.",
    "type": "text",
    "default": "{}"
  },
  "peer_enabled": {
    "description": "Tag incoming messages with peer_id (sender) and commit with self+peer memory_policy, so OV builds a per-person profile under viking://user/<bot>/peers/<sender>/",
    "type": "bool",
    "default": true
  },
  "peer_recall_scope": {
    "description": "Which peers to recall: speaker (self + current speaker) | speaker_plus_active (also recently-active members, default) | none (self only)",
    "type": "string",
    "default": "speaker_plus_active"
  },
  "peer_recall_active_window": {
    "description": "Max number of recently-active members to also recall in speaker_plus_active mode",
    "type": "int",
    "default": 5
  },
  "trusted_mode": {
    "description": "Send X-OpenViking-Account/User identity headers (only for OV servers running auth_mode=trusted behind a gateway). Leave false for standard api_key servers.",
    "type": "bool",
    "default": false
  },
  "auto_recall_enabled": {
    "description": "Auto-recall memories on every LLM request",
    "type": "bool",
    "default": true
  },
  "recall_limit": {
    "description": "Max number of memory entries to recall",
    "type": "int",
    "default": 8
  },
  "recall_min_score": {
    "description": "Minimum semantic score threshold for recall",
    "type": "float",
    "default": 0.35
  },
  "recall_token_budget": {
    "description": "Max tokens for injected recall context",
    "type": "int",
    "default": 2000
  },
  "commit_message_threshold": {
    "description": "Auto-commit after this many pending messages",
    "type": "int",
    "default": 20
  },
  "commit_token_threshold": {
    "description": "Auto-commit when pending tokens exceed this",
    "type": "int",
    "default": 4096
  },
  "commit_idle_seconds": {
    "description": "Auto-commit after this many seconds of inactivity",
    "type": "int",
    "default": 1800
  },
  "ingest_attachments": {
    "description": "Push images/files to OV resources for VLM indexing",
    "type": "bool",
    "default": false
  },
  "capture_tool_io": {
    "description": "Capture LLM tool call inputs/outputs as structured tool parts",
    "type": "bool",
    "default": true
  },
  "capture_image_caption": {
    "description": "Capture AstrBot's image-to-text caption (bot-directed images only) as memory, marked as image-derived. Ignored when caption_all_images is on",
    "type": "bool",
    "default": true
  },
  "caption_all_images": {
    "description": "Actively transcribe EVERY image (incl. ambient group images, not just bot-directed) by calling a vision provider per image. Costs one VLM call per image",
    "type": "bool",
    "default": false
  },
  "image_caption_provider_id": {
    "description": "Provider ID used to transcribe images (empty = AstrBot's default_image_caption_provider_id). Required for caption_all_images",
    "type": "string"
  },
  "image_caption_prompt": {
    "description": "Prompt for image transcription (empty = AstrBot's image_caption_prompt or a built-in Chinese default)",
    "type": "string"
  },
  "backfill_on_first_seen": {
    "description": "Pull historical messages when a venue is first seen",
    "type": "bool",
    "default": true
  },
  "backfill_max_messages": {
    "description": "Max messages to backfill per venue",
    "type": "int",
    "default": 500
  },
  "backfill_max_age_days": {
    "description": "Only backfill messages from the last N days",
    "type": "int",
    "default": 30
  },
  "backfill_batch_size": {
    "description": "Messages per batch when writing backfill to OV",
    "type": "int",
    "default": 20
  },
  "backfill_throttle_ms": {
    "description": "Delay between backfill batches (ms)",
    "type": "int",
    "default": 200
  },
  "bypass_patterns": {
    "description": "Regex patterns to skip OV operations for matching venue IDs",
    "type": "list",
    "items": {
      "type": "string"
    },
    "default": []
  }
}
